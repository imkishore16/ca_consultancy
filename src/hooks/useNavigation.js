// import { useNavigation } from "@react-navigation/native";

// export default function useCustomNavigation() {
//   // Get the navigation object from useNavigation hook
//   const navigation = useNavigation();

//   // Define a function that takes a screen name as an argument and navigates to that screen
//   const goTo = (screen) => navigation.navigate(screen);

//   // Return the goTo function from the custom hook
//   return { goTo };
// }
